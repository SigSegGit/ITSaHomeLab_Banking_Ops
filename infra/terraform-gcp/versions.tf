terraform {
  required_version = ">= 1.6"

  required_providers {
    google = {
      source  = "hashicorp/google"
      version = "~> 6.0"
    }
  }
}

provider "google" {
  project = var.project_id
  region  = var.region
  zone    = var.zone
  # Auth via `gcloud auth application-default login` or
  # GOOGLE_APPLICATION_CREDENTIALS env var — never committed.
}
