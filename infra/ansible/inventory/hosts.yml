---
# A machine that enrolls (bootstrap/enroll.sh) but isn't listed here
# yet falls into `ungrouped` — see ARCHITECTURE.md. Add a machine to a
# real group by moving its hostname into the matching block below; no
# other step is required, the next reconcile pass picks it up.
all:
  children:
    ungrouped:
      hosts: {}
    hypervisor:
      hosts: {}
    k8s_node:
      hosts: {}
    banking_app:
      hosts: {}
