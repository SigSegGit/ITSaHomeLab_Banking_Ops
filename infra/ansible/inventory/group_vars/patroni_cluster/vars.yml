---
# Shared by every node in the Patroni cluster (patroni_db_nodes +
# patroni_witness) via hosts.yml's patroni_cluster parent group.

tailscale_enabled: true

# The owner's real Tailscale tailnet's MagicDNS suffix, confirmed from
# the Tailscale admin console's DNS settings 2026-07-13.
tailscale_tailnet_domain: "tail943111.ts.net"
